{
  "name": "parris-me-uk",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "pretest": "astro sync",
    "test": "vitest run",
    "check": "astro check",
    "ci:build": "npm run check && npm test && npm run build",
    "deploy": "wrangler deploy",
    "cf:dev": "wrangler dev"
  },
  "dependencies": {
    "astro": "^7.3.1",
    "marked": "^18.0.11"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@cloudflare/workers-types": "^5.20260906.1",
    "@resvg/resvg-js": "^2.6.2",
    "@types/node": "^24.13.3",
    "satori": "^0.33.4",
    "typescript": "^6.0.3",
    "vitest": "^5.0.0",
    "wrangler": "^4.130.0"
  }
}
